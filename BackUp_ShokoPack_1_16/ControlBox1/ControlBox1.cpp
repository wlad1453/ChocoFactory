// #include "Cyclogramm.h"

// Cyclogramm::Cyclogramm() {
	
//Cyclogramm::Cyclogramm (uint8_t num) {
/*
void CyclCalc::stopCalc(){
 for (int i = 0; i < ProcessNumber; i++) {
    mProc[i].End = mProc[i].Start + mProc[i].Duration;  // Stop time calculation (mProc[n].End) and signal level setting at the sycle start 
    if (mProc[i].End > cycleLength) {                   // if the process ends in the next cycle
      mProc[i].startLow = false;
      mProc[i].End -= cycleLength;
    }
 }
}*/

/*
void runScreen() {
  lcd.clear(); 
  lcd.setCursor ( 0, 0);  lcd.print( "Cycle    A     E    " );
  lcd.setCursor ( 0, 1);  lcd.print( "         B     F    " );
  lcd.setCursor ( 0, 2);  lcd.print( "Move     C     G    " );
  lcd.setCursor ( 0, 3);  lcd.print( "         D     H    " );
  lcd.noBlink(); lcd.noCursor();
}*/

//}
